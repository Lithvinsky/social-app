# Browser calls API through Vite proxy at /api (default). Override only if needed:
# VITE_API_BASE=http://localhost:5050

# Real-time server (Socket.io). Must match backend URL the browser can reach:
VITE_SOCKET_URL=http://localhost:5050
